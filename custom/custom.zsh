#!/usr/bin/env zsh
# e=emacsclient.sh --no-wait
# $EDITOR=emacsclient.sh
# Use $EDITOR when editing result is piping with other commands
alias zshrc="e ~/.zshrc"
alias av=alias_value
alias zc="e $ZSH_CUSTOM"
alias open=xdg-open
alias src="exec zsh -l"
